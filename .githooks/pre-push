#!/bin/sh
echo "Running TerraSense deployment parity sync & check..."
python backend/scripts/sync_deployment_bundle.py || exit 1
python -m pytest backend/tests/test_deployment_sync.py || exit 1
echo "Deployment bundle parity check PASSED."
exit 0
